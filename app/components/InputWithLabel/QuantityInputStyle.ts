import { StyleSheet } from "react-native";

const QuantityInputStyle = StyleSheet.create({
  input: {
    borderWidth: 1,
    borderColor: "#E8DCB9",
    borderRadius: 20,
    padding: 8,
    marginBottom: 20,
    color: "#fff",
  },
  text: {
    fontSize: 15,
    fontWeight: "bold",
    color: "#FFC145",
    marginBottom: 6,
    textAlign: "center",
  },
});

export default QuantityInputStyle;
